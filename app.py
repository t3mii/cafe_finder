import os
from dotenv import load_dotenv

load_dotenv()


def main():
    # Provider-agnostic placeholder. Implement provider client here (Yelp, Foursquare, etc.).
    print("No provider configured in app.py. Add an API client and call it here.")


if __name__ == "__main__":
    main()